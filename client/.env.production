# Production environment variables
# Loaded when running: npm run build

VITE_API_URL=https://space-is-the-place-api.onrender.com

# Note: l'URL du front (CORS) se configure côté serveur (VITE_CLIENT_URL /
# CORS_ALLOWED_ORIGINS sur Render), pas ici — le client ne la lit jamais.
