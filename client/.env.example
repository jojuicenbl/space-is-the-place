# Example environment variables
# Copy this file to .env for local development

# API Backend URL
VITE_API_URL=http://localhost:3000

# Note: l'URL du front (CORS) se configure côté serveur (VITE_CLIENT_URL /
# CORS_ALLOWED_ORIGINS), pas ici — le client ne la lit jamais.
